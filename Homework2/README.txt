Compile:
	Put the makefile in the same directory as the c files for each program.
	type "make" to compile
	"make clean" will remove the executables and any generated files

Run:
	From the directory the files were compiled:
		stoogesort: "./stoogesort"
			produces stooge.out
		stoogeTime: "./stoogeTime"
			command line output