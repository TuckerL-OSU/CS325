Compile:
	Put the makefile in the same directory as wrestler.cpp
	type "make" to compile
	"make clean" will remove the executable

Run:
	From the directory the file was compiled:
		act: "./wrestler"
			produces command line output