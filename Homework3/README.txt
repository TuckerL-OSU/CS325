Compile:
	Put the makefile in the same directory as shoppingSpree.cpp
	type "make" to compile
	"make clean" will remove the executable and results.txt

Run:
	From the directory the file was compiled:
		shoppingSpree: "./shoppingSpree"
			produces results.txt